import '../../stencil.core';
/** Button that lets the user log in with Solid. */
export declare class LoginButton {
    popup: string;
    webId: string;
    componentWillLoad(): void;
    render(): JSX.Element;
}
